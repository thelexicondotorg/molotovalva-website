# Workflow & Operational Guidelines

## Git Workflow
- **No Automatic Git Actions**: The agent must NOT automatically commit or push changes.
- **Manual Trigger Only**: Commits and pushes must ONLY occur when the USER explicitly requests them.
